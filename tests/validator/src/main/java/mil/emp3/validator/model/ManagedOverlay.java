package mil.emp3.validator.model;

import mil.emp3.api.Overlay;

public class ManagedOverlay extends ManagedObject<Overlay> {

    public ManagedOverlay(Overlay overlay) {
        super(overlay);
    }
}
