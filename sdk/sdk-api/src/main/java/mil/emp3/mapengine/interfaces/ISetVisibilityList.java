package mil.emp3.mapengine.interfaces;

/**
 *
 * This class defines the interface to a list of visibility settings. The UUID
 * is the identifier of the feature the visibility is to be set and the boolean 
 * value the setting.
 */
public interface ISetVisibilityList extends java.util.Map<java.util.UUID, Boolean> {}
